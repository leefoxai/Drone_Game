# 서버 자리

M4에서 Node.js + TypeScript로 기록 업로드, 공유 물리를 이용한 재시뮬레이션 검증,
트랙별 리더보드를 구현한다. 초기 저장은 SQLite + 파일이다.

현재 서버 코드, 데이터베이스, 실행 명령은 없다. 비행 물리는 여기서 구현하지 않고
`packages/physics`를 가져와 사용한다. 비밀 키는 서버 환경 변수로만 관리한다.
