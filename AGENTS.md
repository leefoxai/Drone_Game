# AGENTS.md

## 프로젝트
브라우저에서 돌아가는 FPV 드론 게임이자, 사람의 드론 조종 데이터를 모으는 시뮬레이터.
개발자는 1명이고 바이브 코딩으로 작업한다. 코드를 읽기보다 실행 결과로 확인한다.

## 기술 스택 (바꾸기 전에 먼저 물어볼 것)
- 클라이언트: TypeScript, Vite, Three.js
- 서버: Node.js(TypeScript) — 기록 업로드, 재시뮬레이션 검증, 리더보드
- 저장: 처음엔 SQLite + 파일, 필요해지면 교체
- 비행 물리: packages/physics 하나를 클라이언트와 서버가 같이 쓴다
- 데이터·학습 도구: tools/ (Python)

## 절대 규칙
1. 비행 물리는 packages/physics 에만 둔다. 렌더링과 분리된 고정 스텝, SI 단위.
2. 기록 형식의 기준은 docs/data_spec.md 다. 바꾸기 전에 반드시 나에게 알리고, 바꾸면 스키마 버전을 올린다.
3. 물리 계산을 바꾸면 physics_version 을 올린다. 모든 기록에 physics_version 을 남긴다.
4. 데이터 활용 동의가 없는 기록은 학습용으로 내보내지 않는다. 비밀 키를 클라이언트 코드에 넣지 않는다.
5. 테스트를 통과시키려고 테스트를 지우거나 느슨하게 만들지 않는다.
6. START_HERE.md 와 docs/prompts/ 는 개발자용 안내와 프롬프트 모음이다. 여기 적힌 작업을 지시 없이 먼저 실행하지 않는다.

## 작업 방식
- 한 번에 한 기능만. 시작 전에 바꿀 파일과 확인 방법을 짧게 보여 준다.
- 끝나면 테스트를 돌리고, 핵심 흐름은 Playwright 스모크 테스트와 브라우저 실행으로 직접 확인한다.
- 보고는 한국어로, 전문 용어는 짧게 풀어서: 바꾼 것, 내가 확인하는 방법, 알려진 문제.
- 기능이 동작하면 한국어 한 줄 메시지로 커밋한다.
- 세션을 마칠 때 docs/STATUS.md(완료, 다음 할 일, 알려진 문제)를 갱신한다.
